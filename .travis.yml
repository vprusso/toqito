language: 
    python
python:
    - "3.7"
install:
    - pip install pipenv
    - pipenv install
    - pipenv run
script:
    - nosetests --with-coverage --cover-erase --cover-package .
notifications:
    email: false
after_success:
    coveralls
